'use strict';

angular.module('mswFrontendApp')

    .controller('SettingsCtrl', ['$scope', '$rootScope', '$http', 'dataService', '$location', function ($scope, $rootScope, $http, dataService, $location) {
    }]);
